# Backend Environment Variables

# Server Configuration
PORT=3000

# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/smart-data-connector
# For MongoDB Atlas (cloud), use:
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/smart-data-connector?retryWrites=true&w=majority

# API Configuration (if needed for external APIs)
# API_TIMEOUT=10000
# MAX_RETRIES=3

# CORS Configuration (if needed)
# CORS_ORIGIN=http://localhost:5173

# Logging
# LOG_LEVEL=info

# Mock Server (for testing)
# MOCK_SERVER_PORT=3001
